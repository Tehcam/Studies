<%= form_with(model: artiste, local: true) do |form| %>
  <% if artiste.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artiste.errors.count, "error") %> prohibited this artiste from being saved:</h2>

      <ul>
        <% artiste.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
